import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Header } from "./Header.tsx";

<Meta title="HTML Elements/Header" comoponent={Header} />

export const Template = (args) => <Header {...args} />;

<Story
  name="Headers"
  args={{
    content: "",
  }}
>
  {Template.bind({})}
</Story>
